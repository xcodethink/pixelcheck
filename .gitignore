node_modules/
dist/
reports/
baselines/
tmp/
downloads/
coverage/
docs/perf-current.json
docs/perf-memory.json
docs/api/
test-results/
playwright-report/
sbom.json
*.tgz
.env
.env.*
!.env.example
*.log
*.har
*.webm
.DS_Store

# Claude Code worktrees + other internal scratch directories. Local-only,
# never committed.
.claude/

# Per-operator audit project scaffolding (each subfolder = one user's
# config + personas + scenarios pointing at their app). Operator-private,
# never committed.
projects/

# Internal planning / strategy / marketing notes. Keep these outside the
# repo entirely. The patterns below catch common accident-paths.
PLAN.md
docs/launch-*.md
docs/show-hn.md
docs/research/
docs/archive/
docs/release-notes/
docs/SLO.md
docs/assets/og-*

# Internal Claude Code session work, planning, governance + thinking process.
# NONE of this belongs in a public repo. 2026-07-25: reversed the earlier
# "public governance records" exception — session progress, plans, retros,
# risk registers, release-readiness + handoff protocols are all internal and
# were stripped from the whole GitHub history via git filter-repo.
progress/

# --- internal audit / strategy / commercial review — never public (2026-07-14, extended 2026-07-25) ---
docs/audit/
docs/strategy/
docs/commercial-audit-*.md
